<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Hexo+GitHub建站心得</title>
    <url>/2020/09/04/Hexo+GitHub%E5%BB%BA%E7%AB%99%E5%BF%83%E5%BE%97/</url>
    <content><![CDATA[<h2 id="大体流程"><a href="#大体流程" class="headerlink" title="大体流程"></a><strong>大体流程</strong></h2><h3 id="准备工作"><a href="#准备工作" class="headerlink" title="准备工作"></a><strong>准备工作</strong></h3><p>&emsp;&emsp;准备 node 和 git 环境，首先，安装 NodeJS，因为 Hexo 是基于 Node.js 驱动的一款博客框架，相比起前面提到过的 Jekyll 框架更快更简洁，因为天朝网络被墙的原因尝试过安装 Jekyll 失败而放弃了。然后，安装 git，一个分布式版本控制系统，用于项目的版本控制管理。两个工具不同的平台安装方法有所不一样，可自行了解按步骤安装，这里不详述了。安装成功后打开git bash（Windowns）或者终端（Mac），下方中将统一称为命令行。在命令行中输入相应命令验证是否成功，如果成功会有相应的版本号。<br></p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git version</span><br><span class="line">node -v</span><br><span class="line">npm -v </span><br></pre></td></tr></table></figure>

<p>&emsp;&emsp;打开github，点击New repository，创建一个新仓库，仓库名必须要遵守格式：账户名.github.io，不然接下来会有很多麻烦。注意，Github 仅能使用一个同名仓库的代码托管一个静态站点，并且需要勾选Initialize this repository with a README。<br>在建好的仓库右侧有个settings按钮，点击它，向下拉到GitHub Pages，你会看到有个网址，访问它，你将会发现该项目已经被部署到网络上，能够通过外网来访问它，当然里面还很空什么东西都没有。 该地址就是你的博客默认地址，你也可以购买域名，将其换成你喜欢的地址。</p>
<h3 id="安装Hexo"><a href="#安装Hexo" class="headerlink" title="安装Hexo"></a><strong>安装Hexo</strong></h3><p>&emsp;&emsp;在合适的地方新建一个文件夹，用来存放自己的博客文件。在该目录下右键点击Git Bash Here，打开git的控制台窗口，以后我们所有的操作都在git控制台进行，就不要用Windows自带的控制台了。定位到该目录下，输入</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">npm i hexo-cli -g</span><br></pre></td></tr></table></figure>
<p>安装Hexo。会有几个报错，无视它就行。安装完后输入</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hexo -v</span><br></pre></td></tr></table></figure>
<p>验证是否安装成功。然后就要初始化我们的网站，输入</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hexo init</span><br></pre></td></tr></table></figure>
<p>初始化文件夹，接着输入<br>安装必备的组件。这样本地的网站配置也弄好啦，输入</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hexo g</span><br></pre></td></tr></table></figure>
<p>生成静态网页，然后输入hexo s打开本地服务器，然后浏览器打开<a href="http://localhost:4000/">http://localhost:4000/</a> ，可以看到预览了。<br></p>
<h3 id="连接Github与本地"><a href="#连接Github与本地" class="headerlink" title="连接Github与本地"></a><strong>连接Github与本地</strong></h3><p>首先右键打开git bash，然后输入下面命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git config --global user.name &quot;godweiyang&quot;git config --global user.email </span><br></pre></td></tr></table></figure>
<p>用户名和邮箱根据你注册github的信息自行修改。然后生成密钥SSH key：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ssh-keygen -t rsa -C youremail</span><br></pre></td></tr></table></figure>
<p>打开github，在头像下面点击settings，再点击SSH and GPG keys，新建一个SSH，名字随便。git bash中输入</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cat ~&#x2F;.ssh&#x2F;id_rsa.pub</span><br></pre></td></tr></table></figure>
<p>将输出的内容复制到框中，点击确定保存。输入</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ssh -T git@github.com</span><br></pre></td></tr></table></figure>
<p>，如果如下图所示，出现你的用户名，那就成功了。</p>
]]></content>
      <categories>
        <category>Technology</category>
      </categories>
      <tags>
        <tag>Hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>Hello World</title>
    <url>/2020/09/03/hello-world/</url>
    <content><![CDATA[<p>其实我很早以前就想建一个独立博客，分享我的所见、所闻、所听、所感。但是由于种种原因，直到现在才算是搭了一个乞丐版个人网站。《庄子》云：人生在世，恍若白驹过隙，忽然而已。我的初衷就是把我看到的和听到的记录下来，没有功利性的目的，单纯觉得这是一件很有意义的事情。当然，不选豆瓣简书之类的平台，选择自己搭建，也单纯是觉得这样很酷。<br><br><br>&emsp;&emsp; 这是Geoffrey的博客，我将在这里分享我的学习与生活的经历和感想，也会写写随笔。主要靠兴趣，不定期更新。<br><br><br><img src="/2020/09/03/hello-world/picture.jpg" alt="initiate"></p>
]]></content>
      <categories>
        <category>Experience</category>
      </categories>
      <tags>
        <tag>start</tag>
      </tags>
  </entry>
</search>
